Run this command to install all important packages:

pip install streamlit pandas openai


Set the API keys either in configuration or hard code it in the comment "key" made in code.

run this code in terminal to run the app:

streamlit run app.py
